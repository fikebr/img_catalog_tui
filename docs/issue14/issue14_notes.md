# Issue 14

on the folder page. when the image is horizontal then it extends past the imageset card. this makes the grid of cards stagger in a bad way.

the image on the cards should size to fit the card and the grid of cards needs to be responsive so that the cards do not stagger.